import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    titleHeader:{
        fontSize: 20,
        color: '#8814AB',
        fontWeight: 'bold',
        marginTop: 50,
        marginLeft: '40%',
    },
});