import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textInput:{
        borderColor: '#8814AB',
        borderBottomWidth: 1,
        marginBottom: 10,
        width: '60%',
        height: 40,
        fontSize: 14,
        color: '#212121',
    },
});